import { css } from "styled-components";
export const v = {
  mdWidth: "600px",
  sidebarWidth: "20rem",
};

export const btnReset = css`
  background: none;
  border: none;
  outline: none;
  font-size: 1rem;
  color: inherit;
  font-family: inherit;
  margin: 0;
  padding: 0;
  cursor: pointer;
`;

export const inputReset = css`
  background: none;
  border: none;
  outline: none;
  color: inherit;
  font-family: inherit;
`;
